<!DOCTYPE html>
<html>
  <head>
    <title>{{title}}</title>
    {{csrf}}

    {{stylesheets}}
    {{javascripts}}

    {{icons}}
  </head>
  <body class="{{ body_class }}">
    {{ navbar }}
    <div class="content-fluid">
      {{ body }}
    </div>
  </body>
</html>
