<!DOCTYPE html>
<html>
  <head>
  <title>{{title}}</title>
    {{csrf}}

    {{stylesheets}}
    {{javascripts}}

    {{icons}}
  </head>
  <body>
    <div class="content-fluid">
      {{body}}
    </div>
  </body>
</html>
